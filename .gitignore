# Compiled addon
build/
*.node

# Allow prebuilt platform binaries
!prebuilds/**/*.node

# Auth tokens — never commit
.npmrc

# macOS
.DS_Store

# Temporary / scratch files
/tmp/
*.bak

# VSIX release archives — published directly to GitHub Releases, not committed to git
releases/*.vsix
